# Scripts Directory

This directory is reserved for executable scripts (e.g., Python, Bash) that the agent can run to perform complex validations. 

For the `prompt-corrector` skill, advanced implementations could include a Python script here that calls a local spell-check library or queries a custom user-dictionary database.

*Currently empty as the LLM handles the spell-checking natively via context.*